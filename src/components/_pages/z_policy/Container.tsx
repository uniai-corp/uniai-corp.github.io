import PageContainer from "@/components/page-frame/PageContainer";

export default function PagePolicyContainer({ children }: { children: React.ReactNode }) {
  return <PageContainer>{children}</PageContainer>;
}
